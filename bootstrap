#!/usr/bin/env bash
set -Eeuo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if [[ ! -x "${SCRIPT_DIR}/setup/bootstrap.sh" ]]; then
  echo "Missing setup/bootstrap.sh" >&2
  exit 1
fi

exec "${SCRIPT_DIR}/setup/bootstrap.sh" "$@"

